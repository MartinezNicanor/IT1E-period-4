import './Header.css';

const Header = ({ title }) => {
    return ( 
        <header>
            <p className="headerTitle">{ "Assignments" }</p>
        </header>
     );
}


export default Header;
